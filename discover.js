// discover.js
// TODO:
// - Grab #card-stack and wire up #btn-pass, #btn-superlike, #btn-like
// - Support dragging/swiping the top .rabbit-card with the mouse/touch
// - Animate the card off-screen and reveal .card-badge.like / .nope while dragging
// - Remove the top card from the DOM after a swipe and reveal the next one
// - Show #no-more-cards when the stack is empty
// - Randomly (or based on rules) trigger a match: show #match-modal
// - Wire up #keep-swiping-btn to hide the modal
